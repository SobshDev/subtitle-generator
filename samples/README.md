Drop sample `.mp4` / `.mov` / `.wav` files here for local testing.

These are gitignored — they're for manual end-to-end verification.

A 15–30s talking-head clip is the quickest way to exercise the full
probe → transcribe → edit → render → export pipeline.
